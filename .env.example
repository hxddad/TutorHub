# Replace USER with your PostgreSQL username and PASSWORD with your actual password.
# If your password contains special characters (e.g. #, @, %), URL-encode them (e.g. # → %23).
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/tutorhub"
JWT_SECRET="your-secret-key-change-in-production"
